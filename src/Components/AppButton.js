import React from 'react'
import {Button,Box} from 'native-base';

function AppButton() {
  return (
    <Box>
        <Button>
            Ini Button
        </Button>
    </Box>
  )
}

export default AppButton